@model IEnumerable<Examen_P5_Bram.Flight>
@using Examen_P5_Bram.Translations
@{
    ViewBag.Title = @Texts.HomeFlight;
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Airplane.AirlineId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Airport.Code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Airport1.Code)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Airplane.AirlineId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Airport.Code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Airport1.Code)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
